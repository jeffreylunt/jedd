{
  "name": "jedd-v2",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Jedd V2 — a thin agent loop for the homelab. The tool-call record is the source of truth.",
  "scripts": {
    "chat": "tsx src/index.ts",
    "test": "tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
