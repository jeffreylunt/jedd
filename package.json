{
  "name": "jedd-v2",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Jedd V2 \u2014 a thin agent loop for the homelab. The tool-call record is the source of truth.",
  "scripts": {
    "chat": "tsx src/index.ts",
    "test": "tsx --test --test-timeout=30000 test/*.test.ts",
    "typecheck": "tsc --noEmit",
    "history": "tsx scripts/history.mjs"
  },
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
